# Memory — 2026-03-06 (Friday)

## Session Summary

Long session picking up from prior work. Compaction happened mid-session. Key state captured below.

---

## Mission Control Status

- **Workspace**: `/Users/rex/.openclaw/workspace/mission-control/` (Next.js, port 3000)
- **Convex**: `glorious-clam-523.convex.cloud` (team: `rex-bdc1d`, project: `mission-control`)
- **Dev server**: Running since Wed 6PM, PID 99479 — no restart needed
- **Startup alias**: `mission-control` → runs `start-mission-control.sh`
- **Build**: 0 TypeScript errors, 10 routes compiled, production build passes

## Open Bugs (In Progress)

1. **"What We're Watching" panel not rendering on frontend** — data confirmed present in `/api/marketreport` (10 items, last 4 are sources/conclusion — filter needed); rendering bug in component (wrong field name, conditional guard, or mapping issue)
2. **Asset price cards showing full coin name** — need to compact to `BTC $71,527 -2.4%` format (remove full coin name line, truncate % to one decimal) in `PriceTicker.tsx`
3. **X API $100.00 receipt NOT YET LOGGED** — Invoice 4GWZSREZ-0001, Mar 6 2026, Stripe, card ****7317. Must add to `ai-spend.json` under "X API" provider.
4. **Corrected email to kelly@bitlabacademy.com NOT YET SENT** — prior send (msg_id: 19cc149d155a7be1) had em dash violations. Needs full 7-point gate resend.
5. **Mobile sidebar**: Kelly has not yet confirmed 60px strip with D/H/T/A/O/C letters on iPhone.

## Pending Next Steps

1. Fix "What We're Watching" panel render bug (inspect field names, filter last 4 items)
2. Compact asset price cards in `PriceTicker.tsx`
3. Log X API $100.00 to `ai-spend.json`
4. Resend corrected email to kelly@bitlabacademy.com (zero em dashes, full gate)
5. Kelly loads X API credits ($20-50 starting balance)
6. Kelly provides list of X accounts to monitor (crypto journalists, analysts, influencers)
7. Build Node.js X RSS adapter on Railway (Bearer Token as env var)
8. Wire Railway URL into `convex/headlines.ts` RSS_SOURCES
9. Design cron-based scheduling pattern — document in AGENTS.md
10. Wire "Update Headlines" button to live news-aggregation skill
11. Build `/tasks` page — full task queue view, status filters, detail expansion
12. Set up Tailscale for remote access
13. Newsletter Automation Agent (Rock 2)
14. Google Calendar OAuth setup

## X API Info

- Developer account: "xrex kellam" (throwaway Gmail + X)
- Production environment app created
- Bearer Token saved by Kelly (NOT in any Rex file per LAW 6)
- Basic subscription: $100.00 charged Mar 6, 2026 (Invoice 4GWZSREZ-0001, card ****7317)
- Credits-based billing — Kelly needs to load $20-50 to start
- Plan: Node.js adapter on Railway, exposes RSS feed, 15-min cache

## Spend Tracking (ai-spend.json)

- OpenRouter: $52.97 (Mar 4, Receipt #2702-1990, Invoice KVZPXYQE-0002, card 7317)
- Anthropic: $25.00 (Mar 2), $50.00 (Mar 3), $50.00 (Mar 4) — Invoices 7DROASAF-0005/006/007
- X API: $100.00 (Mar 6, Invoice 4GWZSREZ-0001) — **NOT YET IN FILE**
- All-time total: ~$277.97 (once X API logged)

## Market Data (March 5, 2026)

- BTC: $71,680 (+8.02%)
- ETH: $2,081 (+7.26%)
- BlackRock IBIT: $767M inflow
- Kraken: first Fed master account
- Morgan Stanley: spot BTC ETF filing
- Fear & Greed: 19

## Standing Architecture Decisions

- `rex-notes.md` = canonical normalized market intelligence, newest entry at top
- `dataTimestamp` = midnight UTC of `## YYYY-MM-DD` section header (NOT email arrival)
- Pacific Time: `America/Los_Angeles` handles PST→PDT automatically
- Tier system: 0 (filtered), 1 (always), 2 (significant), 3 (major only — hidden by default)
- Pure inline CSS for progress bars (Tailwind v4 conflict)
- `agentCodename` not `agent` in Convex schema
- Sub-agent proof = Step 1 before any written deliverable
- No sleeping sub-agents for scheduled tasks (OpenClaw kills multi-hour `runTimeoutSeconds`)
- Zero em dashes in ANY written content — no exceptions except pull-quote attribution
- Gate mechanism must explicitly list all 7 checklist items with ✅/❌
- All emails sign off as **Rex**, never Forge or any codename

## Error Journal

- PR-001 through PR-008 logged
- PR-001 marked as recurred (em dash violation happened twice)
- Journal: `skills/error-journal/references/journal-log.md`
- Rules: `skills/error-journal/references/preventive-rules.md`

## System Config

- OpenClaw Gateway Port: `30322`
- Model: `openrouter/qwen/qwen3.5-35b-a3b` — stay here until Kelly says otherwise
- Kelly Telegram chat ID: `1011362712`
- Kelly pronouns: he/him
- Kelly timezone: PST (America/Los_Angeles)

## Q2 2026 Rocks (Mar 3 – Jun 1)

1. AI Content Pipeline — 5+ pieces/week across Shorts, Reels, TikTok
2. Paid Newsletter — $10K MRR by June 1 (Substack @discovercrypto, $15/mo | $99/yr)
3. Digital Products — ebook + TradingView Pine Script indicator
4. Members Community App MVP — live news, prices, on-chain signals, education

## Discover Crypto Stats

- YouTube: 1.4M subs | 4.5M views/28 days
- X: 1M+ followers
- Instagram: 500K+ followers
- Discord: 3,700 members

## Active Sponsors

- Bitcoin IRA: $10K/mo
- LBank: $20K/mo
- Arculus: $12K/mo
- Total MRR: $42K/mo

## 28 Skills — All Deployed ✅

All 28 skills built and loaded. All updated with visible pre-action gates (LAW 7). Sub-agent proof protocol added to humanization-voice, email-assistant, x-post-automator, article-writing skills.

## RSS Sources (current)

- CoinTelegraph, CoinDesk, The Block, Bitcoin Magazine, Yahoo Finance
- Decrypt: permanently removed (too noisy)

## Key Files

- `market-notes.md`: `/Users/rex/.openclaw/workspace/market-reports/market-notes.md`
- `rex-notes.md`: `/Users/rex/.openclaw/workspace/rex-notes.md`
- `ai-spend.json`: `/Users/rex/.openclaw/workspace/ai-spend.json`
- `GMAIL_HOOK.md`: `/Users/rex/.openclaw/workspace/GMAIL_HOOK.md`
- Error journal: `/Users/rex/.openclaw/workspace/skills/error-journal/references/journal-log.md`
- Preventive rules: `/Users/rex/.openclaw/workspace/skills/error-journal/references/preventive-rules.md`
- Skill registry: `/Users/rex/.openclaw/workspace/mission-control/config/skill-registry.ts`
- Sidebar: `/Users/rex/.openclaw/workspace/mission-control/components/layout/Sidebar.tsx`

## X Feed TweetCard Layout Update (2026-03-06 ~1PM)

Rearranged tweet card layout in `/Users/rex/.openclaw/workspace/mission-control/app/x-feed/page.tsx`:

**New layout:**
- **Top row**: `[🐦 @username · time ago]` ——— `[Rewrite button]`
- **Bottom row**: `[Read more ▼]` ——————— `[View on X ↗]`

**Key implementation details:**
- Spacer `<div style={{ flex: 1 }}>` pushes Rewrite button to far right in top row flex container
- `{expandable ? <button> : <span />}` keeps `justify-between` layout intact when no expand toggle (so "View on X" stays right-aligned regardless)
- Bottom row separated by `borderTop` divider
- Both `TweetCard` and `HeadlineAlertCard` components updated
- Icons: `Twitter`, `ExternalLink`, `CheckCircle2` from lucide-react

**Pending verification:**
- Kelly has not visually confirmed this renders correctly in browser (Chrome extension not attached, browser tool unavailable)
- Confirm `HeadlineAlertCard` got same treatment (mentioned but only one edit shown in session)

---

## 7 Laws (AGENTS.md)

1. LAW 1 — Humanization Framework (always active)
2. LAW 2 — Session Continuity (session-handoff.md on goodbye)
3. LAW 3 — 5-Min Inactivity (update session-handoff.md proactively)
4. LAW 4 — Injection Defense (always active)
5. LAW 5 — Anti-Hallucination (zero fabrication)
6. LAW 6 — Security (no credentials in files)
7. LAW 7 — Universal Output Gate (Sub-agent PASS = Step 1 before any written deliverable)

---

## Afternoon Session Work (March 6, 2026 — ~1PM to 6PM PST)

### Fixes Shipped

**Fix 4 — Real dashboard timestamps (DONE)**
- Dashboard agent cards now show real Convex `updatedAt` timestamps instead of hardcoded strings
- Added `agentLastTask` useMemo computed from `tasks.getAll` — filters by `agentCodename`, takes max `updatedAt`
- Fixed `task.agent` → `task.agentCodename` bug in task queue panel (was showing undefined)
- Added `relTime()` helper to dashboard

**Fix 3 — Headlines cron health indicator (DONE)**
- `CronStatus` component added to Headlines page header
- Green dot = synced <20min | Yellow = 20-60min | Red = >1hr or no data
- Wired to existing `lastFetchedAt` Convex query — no extra queries

**Fix 6 — Memory page (DONE)**
- `/app/memory/page.tsx` — read-only file browser for MEMORY.md + memory/YYYY-MM-DD.md
- `/app/api/memory/route.ts` — reads workspace files, returns JSON
- Shows 6 files: 1 long-term (13KB) + 5 daily logs
- Markdown renderer (headings, bold, code, bullets, HR) — no external library
- Stat cards: file count, daily count, total KB
- "Memory" added to sidebar nav (Brain icon, "M" on mobile)
- API verified: `ok=true files=6` ✅

**X Feed infinite loop fix (DONE)**
- `toast` in `useToast()` was not wrapped in `useCallback` → recreated every render → `fetchFeed` (depends on toast) also recreated → `useEffect([fetchFeed])` re-fired → infinite fetch loop → page flashing
- Fix: `const toast = useCallback((m) => {...}, [])` — stable reference, loop broken

**2-line CSS clamp locked in everywhere (DONE)**
- X Feed `TweetCard` + `HeadlineAlertCard`: CSS `WebkitLineClamp` with `useRef` to detect actual clamping; "Read more ▼" only shows when needed
- Headlines `HeadlineCard` description: 2-line clamp when collapsed, full when Deeper Insight open
- Kanban `Card.tsx`: already had Tailwind `line-clamp-2` ✅
- `clampStyle(n)` function replaces static `CLAMP_STYLE` constant — supports any line count

**Tailscale / Mission Control remote access fix (DONE)**
- `next dev` was binding to `127.0.0.1` only — Tailscale IP `100.70.46.41` was unreachable
- Fix: `"dev": "next dev -H 0.0.0.0"` in package.json — now listens on all interfaces
- Startup script updated to show both URLs
- Server restarted — verified 200 on both `localhost:3000` and `100.70.46.41:3000`
- Kelly confirmed iPhone loads Mission Control over 5G with Tailscale ✅

**Mobile X Feed accordion (DONE)**
- `useIsMobile()` hook (defaults false on SSR, checks `window.innerWidth < 768`)
- `MobileSection` type: `"prices" | "breaking" | "btcEth" | "macro" | "altcoins" | null`
- `toggleSection(s)` — single state value = automatic accordion (clicking one closes all others)
- PRICES button → `MobilePriceList` (ticker + price only, no %)
- BREAKING button → X Headlines, single column, **1-line clamp** (half of desktop 2-line)
- BTC/ETH / MACRO / ALTS buttons → vertical group; posts expand below all 3 buttons in single column
- Accent colors match desktop (orange/purple/green) on each button
- Desktop: zero changes — full layout unchanged
- Kelly: "Mobile X FEED page looks incredible" ✅

**Mobile Headlines accordion (DONE)**
- Same `useIsMobile` + `HeadlineMobileSection` type + `MobileAccordionButton` pattern
- 4 sections: PRICES / DAILY INSIGHTS / WATCHING / STORIES
- PRICES → same `MobilePriceList` (ticker + price only)
- DAILY INSIGHTS → intro + Market Pulse rendered inline
- WATCHING → watching items list with title/body
- STORIES → full headlines list with **mobile card layout fix**
- Mobile story card layout: buttons moved from right column → bottom row full-width
  - `ActionBtn` got `fullWidth?: boolean` prop
  - `HeadlineCard` got `isMobile?: boolean` prop
  - Desktop layout: unchanged (buttons still in right column)
- Deeper Insight toggle still works within each mobile story card

### New Rule Added
- **85% Context Warning Rule**: When context hits 85%, immediately alert Kelly, log current state to memory, and WAIT for Kelly's signal before compaction. Do NOT auto-compact without Kelly's approval.

### Mission Control Current State
- **Routes**: 11 total (added /memory)
- **Nav items**: D/H/X/T/A/O/C/M (8 items)
- **Tailscale URL**: http://100.70.46.41:3000
- **Dev server**: Running on 0.0.0.0:3000
- **Zero TypeScript errors** ✅

### Still Pending (from earlier session)
- Fix 5 — Rock % updates on Ops page: Kelly hasn't answered (inline edit control vs manual Monday?)
- Fix 1 — Task executor: X rewrites = Option A; other task types TBD via Q&A
- Fix 7 — Agent uptime: needs write-side heartbeat before read-side makes sense
- Compact asset price cards in PriceTicker.tsx (remove full coin name)
- Log X API $100.00 receipt to ai-spend.json (Invoice 4GWZSREZ-0001, Mar 6, card 7317)
- Resend corrected email to kelly@bitlabacademy.com (zero em dashes)

---

## Resend Email Integration — Paused (2026-03-06 ~7:19 PM PST)

### What We Built
- `/app/api/calendar-invite/route.ts` — Resend email route, fully built and working
- `RESEND_API_KEY=re_KePC7HAo_NNCCE5Dfdx3hDixJ2nYWmGEv` — added to `.env.local`
- Calendar page `handleSubmitForm` — fires `/api/calendar-invite` after task creation if email field is filled
- Email is a branded HTML invite: dark navy theme, task name, date/time, description, Hit Network footer

### What Works Right Now
- Creating a task with email invite saves to Convex with `notes: "INVITE: email@..."` ✅
- `/api/calendar-invite` calls Resend API and sends email ✅
- BUT Resend free tier only allows sending to the account owner's email until a domain is verified

### What's Blocked
- **Domain verification for `hitnetwork.io` on Resend** — requires adding DNS records to GoDaddy
- `hitnetwork.io` DNS is on **GoDaddy** (`ns59.domaincontrol.com` / `ns60.domaincontrol.com`)
- Kelly does NOT have GoDaddy admin access — network administrator holds it
- Need network admin to add 2-3 DNS records from Resend dashboard

### Exact Steps for Tomorrow
1. Kelly logs into **Resend dashboard** → Domains → Add Domain → `hitnetwork.io`
2. Resend generates DNS records (SPF + DKIM, typically 2-3 records)
3. **Network admin** (or Kelly with GoDaddy access) adds those records to GoDaddy DNS for `hitnetwork.io`
4. Hit Verify in Resend → domain confirmed
5. Rex updates `from:` in `/api/calendar-invite/route.ts`:
   - Change `"Hit Network <onboarding@resend.dev>"` → `"Hit Network <calendar@hitnetwork.io>"`
6. Test: create a calendar task with any external email → confirm invite arrives
7. Optionally: send from `rex@hitnetwork.io` for Rex-branded invites

### Current From Address (Testing Only)
`onboarding@resend.dev` — works for sending to `rex@hitnetwork.io` only

### Key Files
- Route: `/Users/rex/.openclaw/workspace/mission-control/app/api/calendar-invite/route.ts`
- Env: `/Users/rex/.openclaw/workspace/mission-control/.env.local` (RESEND_API_KEY)
- Calendar page: `/Users/rex/.openclaw/workspace/mission-control/app/calendar/page.tsx` (handleSubmitForm)

### Tonight — Digital Product Research Pipeline (ACTIVE)

### What's Running
Two research sub-agents launched ~8:15 PM PST researching all 10 product ideas.
8am cron scheduled (ID: 82d8ff45) to deliver morning brief to Kelly via Telegram.

### Pipeline
1. Sub-agent A: research-ideas-1-5.md (running)
2. Sub-agent B: research-ideas-6-10.md (running)
3. Synthesis agent (pending — spawn when both files complete)
   - Prompt saved: /Users/rex/.openclaw/workspace/product-research/SYNTHESIS-PROMPT.md
4. Slide deck builds: /app/slides/[product-slug]/page.tsx (3 decks x 15 slides)
5. Morning brief file: /Users/rex/.openclaw/workspace/morning-brief-20260307.md
6. Cron delivers at 8:00 AM PST March 7

### Key Files
- research-ideas-1-5.md: product-research/
- research-ideas-6-10.md: product-research/
- synthesis-top3.md: product-research/ (after synthesis)
- SYNTHESIS-PROMPT.md: product-research/ (ready to use)
- morning-brief-20260307.md: workspace root (write last)

---

## Tomorrow — Kelly Brain Dump Session (PRIORITY)

Kelly wants to sit and talk — life, goals, Hit Network direction, personal ambitions, exciting ideas. Not a task session. A real conversation.

**Purpose:** Get Rex deeply in tune with Kelly's thinking so ideas become sharper, more personal, and more proactive. Less task-response, more direction-generation.

**Rex's role:** Listen. Ask good questions. Dig into the "why" behind things. Let the conversation spark ideas naturally — don't force it.

**Format:** Kelly talks, Rex asks questions, explores threads, then starts connecting dots and generating directions Kelly hasn't thought of yet.

**Note to self:** Come in with zero agenda. Just be present and curious.

---

## Future Project — Mission Control Portable Framework

Kelly asked about exporting Mission Control as a deployable framework for other org members. Full analysis done. Decision: **noted for future, no action now.**

Key notes:
- ~70% of the framework is already portable (UI shell, task system, calendar, agents, memory, ops structure)
- ~30% is Hit Network specific (headlines pipeline, X Feed, price assets, skill registry, agent codenames)
- Path to portable: extract hardcoded values into `config/` directory + `.env.example` + `/setup` onboarding wizard
- Deploy path: clone repo → `npx convex dev` → fill `.env.local` → run `/setup` wizard → done
- Estimated build effort: ~1 focused day once we're ready
- Will revisit when Kelly identifies an org member to deploy to

---

## Resend Account
- Signed up via GitHub (rexclaw26 GitHub account)
- Free tier: 3,000 emails/month
- API Key: stored in .env.local (never in code)
