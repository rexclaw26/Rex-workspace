# 2026-03-25 — Session Log

## Projects Worked On

### DC Data Hub (rexclaw26/dc-data-hub)
Railway: https://dc-data-hub-production-cff0.up.railway.app
Repo: rexclaw26/dc-data-hub (branch: main)
CLI service ID: a98ccb85-44f8-49e6-b314-e1d323e3695d (dc-data-hub-production)

**What changed today:**

1. **Deeper Insight fix** — HeadlinesSection: replaced stale marketnotes bullet block with LLM insight API call. Removed `extractRelevantContext` / `marketnotes` / `fmtInsightDate`. Now shows only synthesized LLM insight. Build failed initially due to `item._id` TypeScript error (Headline type only has `url`), fixed to `item.url`.

2. **GitHub/Railway sync confusion** — Railway was watching `main` branch but code was pushed to `master`. Also, there are TWO Railway services: `dc-data-hub` (linked to local `mission-control` code) and `dc-data-hub-production-cff0` (the actual live site). CLI deploys target service by ID. Must use `--service "a98ccb85-44f8-49e6-b314-e1d323e3695d"` for production.

3. **X Feed card — no duplicate posts** — XFeedCard: duplicate fix. Changed from always picking `[0]` per category to tracking `used` Set so each preview slot shows a distinct post.

4. **Mobile accordion (main page)** — page.tsx: added `useIsMobile` hook (768px breakpoint), `MobileSection` type, `MobileAccordionButton`. Desktop unchanged. Mobile: accordion with one section open at a time. Applied to all 9 sections + 2 Coming Soon cards.

5. **XFeedCard extracted** — moved inline XFeedCard to `src/components/XFeedCard.tsx` with internal mobile accordion (desktop: stacked list, mobile: accordion per category within the card).

6. **X Feed page mobile accordion** — `src/components/sections/XFeedSection.tsx`: replaced mobile tab bar with accordion. Added `useIsMobile` hook, `mobileOpen` state (starts null = all collapsed). Categories: Breaking / BTC-ETH / Macro / Altcoins. Each collapses to a button, tap to expand. Desktop unchanged (4-column grid).

7. **Title fix** — expanded "Deeper Insight" panel now shows "Discover Crypto Insights" (plural, static) instead of date/category subtitle.

**Key lesson:** DC Data Hub has TWO codebases:
- `~/openclaw/workspace/mission-control/` — Mission Control app (not related to DC Data Hub despite some shared code patterns)
- `/tmp/dc-data-hub/` — actual DC Data Hub source (git clone of rexclaw26/dc-data-hub, branch main)

### Google OAuth
- Key rotated (sk-or-v1-...) — stored in Railway env vars for dc-data-hub

## Blockers / Notes
- bitcointreasuries.net API returns >2MB responses — Next.js can't cache them. Cache warnings are harmless.
- `mission-control` directory is inside the openclaw-setup git repo, not its own repo
- Railway CLI needs `railway link --project "dc-data-hub"` each session to connect

## Errors & Failed Attempts

1. **Build failed: `item._id` doesn't exist** — HeadlinesSection.tsx used `item._id` but Headline type only has `url`. Fixed: changed to `item.url`. Also added HTTP status check and removed duplicate error catch block.

2. **Wrong codebase deployed** — Accidentally ran `railway up` from `~/openclaw/workspace/mission-control/` directory, overwriting the DC Data Hub production site with Mission Control code. Caused "Application error" crash on dc-data-hub. Had to redeploy from correct `/tmp/dc-data-hub` clone.

3. **Wrong Railway service ID** — Multiple `railway up` attempts failed until I found the correct service UUID. Tried `dc-data-hub`, `dc-data-hub-production`, `dc-data-hub-production-cff0` (name) before finding the numeric UUID `a98ccb85-44f8-49e6-b314-e1d323e3695d` from Railway's URL.

4. **Two Railway services, same project** — `dc-data-hub` (default CLI-linked, built from mission-control code) and `dc-data-hub-production-cff0` (the actual live site). Must always specify `--service "a98ccb85-44f8-49e6-b314-e1d323e3695d"` for DC Data Hub production deploys.

5. **Confused Mission Control with DC Data Hub multiple times** — Kelly explicitly corrected me: "We have been working all day on the DC Data Hub. Leave Mission Control alone." I kept referencing `/headlines` page and other Mission Control code paths. Had to be repeatedly redirected.

6. **X Feed accordion applied to wrong page** — Applied mobile accordion to XFeedCard on main DC Data Hub page, then XFeedSection on `/sections/x-feed` — but what Kelly actually wanted was the `/sections/x-feed` page which I initially missed entirely. Took 3+ attempts to understand the target URL.

7. **`color-mix()` CSS used without fallbacks** — Accordion button colors used `color-mix(in srgb, ${cat.accentHex} 12%, transparent)` inline style. Tailwind handles this but inline styles may not on all browsers. Acceptable for MVP.

8. **`git push` failed initially** — repo `rexclaw26/mission-control` doesn't exist. Pushed to `rexclaw26/dc-data-hub` instead (the correct repo for DC Data Hub).

## Pending
- Enable GitHub auto-deploy on Railway dashboard (Kelly to do in Railway UI)
- Mission Control: not actively worked on — unrelated to DC Data Hub

## Rule Changes
- PR-049 (2026-03-25): Error troubleshooting protocol — dual-subagent diagnosis + root-cause-first fix + mandatory critic before implementing. PERMANENT. Triggered by: Kelly nearly walked from OpenClaw after ~7 hours of repeated gateway hangs requiring pkill/restart cycles.
- PR-050 (2026-03-25): When Kelly references a URL, verify the URL responds and identify the correct source file BEFORE touching any code. Never assume from context.

## [23:15 PDT] Nightly MC Review — Pipeline Run

**Status:** Degraded (2 of 4 assessment agents completed)
- Business Consultant: ✅ Full results
- Project Manager: ✅ Full results  
- UX Designer: ❌ Timed out
- Systems Engineer: ❌ Timed out (was still locating MC codebase)

**3 Proposals synthesized + sent to Telegram (chat 1011362712):**
1. Task Impact Ledger — outcome fields on tasks, weekly review to tag ROI
2. Handoff Dashboard Widget — surface session-handoff.md on Dashboard page
3. Spend Pacing & ROI Tracker — forward-looking budget alerts + per-skill ROI

**Recommended first build:** Proposal 2 (Handoff Widget — lowest complexity, highest daily friction reduction)

**Logged to:** memory/nightly-mc-proposals.md
